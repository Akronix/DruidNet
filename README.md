# DruidNet
The knowledge of the Druid now in your phone!

## Setup

To use the identification from PlantNet, you need to get an API key from Pl@ntNet and set the variable PLANTNET_API_KEY in a local.properties file at the root of the project.
